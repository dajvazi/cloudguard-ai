# Copy to .env in the project root for docker-compose variable substitution.
# AWS/OpenAI secrets are loaded from backend/.env (see docker-compose.yml env_file).

POSTGRES_PASSWORD=cloudguard

# Optional overrides (backend/.env is the main source for AWS + OpenAI):
# OpenAI__ApiKey=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_EC2_INSTANCE_ID=
# AWS__SsmEnabled=true
# AWS__AutoHealEnabled=true
